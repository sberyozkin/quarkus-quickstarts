quarkus.oidc.auth-server-url=${keycloak.host}/realms/software-credentials
quarkus.oidc.client-id=software-credentials-wallet
quarkus.oidc.credentials.secret=uArydomqOymeF0tBrtipkPYujNNUuDlt
quarkus.oidc.application-type=web-app
quarkus.oidc.authentication.cookie-path=/software-credentials-academy
quarkus.oidc.authentication.scopes=profile
quarkus.oidc.tenant-paths=/software-credentials-academy/*
quarkus.oidc.authentication.force-redirect-https-scheme=${force.redirect.https.scheme:false}
quarkus.oidc.token.age=1H

# To workaround a mismatch between NGrock and localhost domains and simplify a demo
quarkus.oidc.token.issuer=any
quarkus.oidc.tls.verification=NONE

quarkus.tls.keycloak.trust-all=true
quarkus.tls.keycloak.hostname-verification-algorithm=NONE

quarkus.oidvc.credential-issuer-url=${keycloak.host}/realms/software-credentials
quarkus.oidvc.tls-configuration-name=keycloak

# mailer config
quarkus.mailer.from=transaction-codes@software-credentials-academy.com

wallet.host=${wallet.domain:http://localhost:8080}
verifier.host=${verifier.domain:http://localhost:8082}
keycloak.host=${keycloak.domain:https://localhost:8443}
issuer.host=${issuer.domain:http://localhost:8081}
